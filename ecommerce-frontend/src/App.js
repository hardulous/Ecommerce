import React from "react";
import UserRoutes from "./Components/Users/UserRoutes";


function App() {

  return (
     
    <UserRoutes/>  // it contains Routing for signin,signup

  );

}

export default App;

// install package react-router-dom

// if you are creating an env variable for react_app start in that file start writting by first write "React_app" , then name of the variable